# Hoot Environment Variables
# These are ONLY needed for production deployment
# Local development works without any .env file

# For Railway deployment:
# Backend service:
PORT=8008
FRONTEND_URL=https://your-frontend-url.railway.app
HOOT_SESSION_TOKEN=generate-a-secure-random-token

# Frontend service (Railway will auto-inject this):
VITE_BACKEND_URL=https://your-backend-url.railway.app
